apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
- service.yaml

configMapGenerator:
  - name: dhk-ambulance-webapi-mongodb-init
    files:
      - params/init-db.js
  - name: dhk-ambulance-webapi-config
    literals:
      - database=dhk-ambulance-wl
      - collection=performance_records
patches:
- path: patches/webapi.deployment.yaml
  target:
    group: apps
    version: v1
    kind: Deployment
    name: dhk-ambulance-webapi